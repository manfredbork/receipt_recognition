name: receipt_recognition
description: "A receipt recognizer based on Google's ML Kit."
version: 0.0.2
homepage: https://github.com/manfredbork/receipt_recognition
repository: https://github.com/manfredbork/receipt_recognition

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  google_mlkit_text_recognition: ^0.15.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
